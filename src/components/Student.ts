interface Student {
    id: string ,
    name?: string,
    surname?: string,
    birthDate?: string,
    courseNumber?: number,
    speciality?: string,
    registerDate?: Date,
}

export default Student;